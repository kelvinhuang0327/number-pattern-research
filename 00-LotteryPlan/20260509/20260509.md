# LotteryNew — Replay Lifecycle 每日工程交接報告

## 1. 本輪目標

本輪目標是延續 Replay Lifecycle workstream，在 PR #3～#11 已完成基礎設施、browser E2E、catalog truth boundary、empty-state honesty 後，進一步處理：

```text
P1 REJECTED Canonical Promotion Plan
```

重點不是直接修改 registry 或 production catalog，而是先把 `rejected/` 內既有 archive evidence 盤點成可審查的 dry-run promotion plan，判斷哪些 REJECTED archive rows 具備 canonical promotion 條件，哪些仍需補 schema / 修資料。

本輪明確禁止：

```text
不寫 production DB
不執行 migration
不新增 catalog row
不修改 registry / Replay UI / API
不改 active strategy state
不做 replay generation
不做 strategy mining / edge discovery
```

---

## 2. 已完成事項

### A. P1 REJECTED evidence inventory 已完成

已讀取：

```text
rejected/README.md
rejected/*.json
wiki/system/replay_data_hygiene.md §4
outputs/replay/p0_replay_lifecycle_catalog_truth_inventory_20260509.md
outputs/replay/p0_replay_lifecycle_catalog_population_plan_20260509.md
```

本輪盤點結果：

| 分類               | 數量 | 說明                                                           |
| ---------------- | -: | ------------------------------------------------------------ |
| Promotable       | 15 | 已具備必要 schema，可作 dry-run canonical REJECTED 候選                |
| Blocked          | 26 | 缺必要欄位，需後續 schema backfill                                    |
| JSON parse error |  1 | `p1_deviation_2bet_539.json` 需人工修復                           |
| Total            | 42 | 對齊 PR #10 catalog truth boundary 的 REJECTED archive evidence |

Promotable 分布：

```text
BIG_LOTTO = 13
POWER_LOTTO = 2
DAILY_539 = 0
```

### B. Canonical promotion criteria 已定義

已建立 REJECTED archive evidence → canonical REJECTED row 的 promotion criteria，包含：

```text
required fields
source path
rejection reason
effective date
audit marker
data_scope
forbidden-language compliance
rollback criteria
no edge claim
no recommendation wording
```

### C. Dry-run manifest 已產出

已建立：

```text
outputs/replay/p1_replay_lifecycle_rejected_canonical_promotion_manifest_20260509.json
```

此 manifest 僅為 dry-run：

```text
不寫 DB
不改 registry
不新增 catalog row
不執行 migration
```

### D. Promotion plan report 已產出

已建立：

```text
outputs/replay/p1_replay_lifecycle_rejected_canonical_promotion_plan_20260509.md
```

報告內容包含：

```text
Evidence Inventory
Canonical Promotion Criteria
Dry-run Manifest Summary
API / UI Impact Plan
Forbidden-Language Sweep Result
Validation Result
Remaining Risks
Recommended Next Action
```

### E. Commit / Push 已完成

已 commit 並 push：

```text
Branch:
codex/p1-replay-lifecycle-rejected-canonical-promotion-plan

Commit:
4ff5adc

Commit message:
docs(replay): plan rejected lifecycle canonical promotion
```

最終 marker：

```text
P1_REPLAY_LIFECYCLE_REJECTED_CANONICAL_PROMOTION_PLAN_READY
```

以上成果已由上一輪執行紀錄確認，包含 42 JSON evidence 盤點、15 promotable / 26 blocked / 1 parse error、manifest/report 產出、validation 與 commit/push 結果。

---

## 3. 修改或產出的檔案

### 新增 / 產出

```text
outputs/replay/p1_replay_lifecycle_rejected_canonical_promotion_plan_20260509.md
outputs/replay/p1_replay_lifecycle_rejected_canonical_promotion_manifest_20260509.json
```

### 未修改

```text
index.html
lottery_api/routes/replay.py
lottery_api/models/replay_strategy_registry.py
tests/**
.github/workflows/**
*.db / *.db-wal / *.db-shm
active strategy state files
branch protection settings
```

---

## 4. 驗證結果 / 測試結果

### Validation

| 驗證項目                                          |   結果 | 備註                                 |
| --------------------------------------------- | ---: | ---------------------------------- |
| `scripts/run_replay_ci_default_validation.py` | PASS | `57 passed, 32 skipped, 1 warning` |
| `git diff --check`                            | PASS | clean                              |
| Forbidden-language sweep                      | PASS | HIGH=0                             |
| DB write / registry change check              | PASS | 無修改                                |
| Branch protection change check                | PASS | 無修改                                |

### Manifest / evidence validation

| 項目                              |           結果 |
| ------------------------------- | -----------: |
| REJECTED archive rows total     |           42 |
| Promotable                      |           15 |
| Blocked missing required fields |           26 |
| JSON parse error                |            1 |
| Dry-run only                    |         PASS |
| Production promotion            | NOT EXECUTED |

---

## 5. 目前結論

本輪已成功把 PR #10 中「REJECTED = 42 archive evidence rows」往前推進一層，從單純 truth inventory 變成 **可審查的 canonical promotion dry-run manifest**。

目前結論是：

```text
REJECTED canonical promotion 尚未執行，但已完成候選清單與阻塞清單。
```

可推進項目：

```text
15 rows 可進入 CTO review / approval
```

仍不可直接推進項目：

```text
26 rows 缺必要 schema，需要 backfill 計畫
1 row JSON parse error，需要人工修復
DAILY_539 目前 0 rows 可 promote
```

這代表目前不應直接寫 registry 或 production catalog，而應先進行 CTO review，確認 15-row dry-run manifest 是否可作下一輪實作基礎。

---

## 6. 尚未完成事項

### P0 / 立即待處理

* [ ] CTO review 15-row dry-run manifest。
* [ ] 確認 15 rows 是否全部適合 canonical REJECTED promotion。
* [ ] 修復 `p1_deviation_2bet_539.json` parse error。
* [ ] 決定 26 blocked rows 是否進入 schema backfill。
* [ ] 若 CTO approve，建立下一輪 **P1 execution PR**，但仍需先做 spec / tests。

### P1 / 後續工程

* [ ] 把 approved REJECTED rows 加入 registry / catalog 的實作設計。
* [ ] 增加 drift guard，確保 REJECTED canonical rows 不會與 archive evidence 漂移。
* [ ] 更新 REJECTED tab UI 行為，從 empty-state 轉成 canonical rows display。
* [ ] 補 API contract tests：`lifecycle_status=REJECTED` 應回傳 canonical REJECTED rows。
* [ ] 若 REJECTED rows 被 canonical 化，再更新 browser E2E expectations。

### P2 / 後續資料治理

* [ ] Schema backfill plan for 26 blocked rows。
* [ ] Daily 539 rejected archive 的 schema 補強。
* [ ] JSON parse-error file repair。
* [ ] Provenance schema design / audit trail。

---

## 7. 風險與不確定點

| 風險                                     |   等級 | 說明                                         | 建議                              |
| -------------------------------------- | ---: | ------------------------------------------ | ------------------------------- |
| 15 rows promotable 仍需 CTO 審核           | HIGH | Dry-run manifest 不等於 production approval   | 下一輪先做 PR readiness / CTO review |
| 26 rows blocked                        |  MED | README 可能有資料可 backfill，但本輪未授權推論            | 另開 P2 schema backfill           |
| 1 JSON parse error                     |  MED | `p1_deviation_2bet_539.json` 無法納入 manifest | 單獨修 parse error                 |
| Daily 539 promotable = 0               |  MED | 可能導致 REJECTED tab canonical 化後仍偏彩票種        | 補 schema 前不要 overclaim          |
| Archive evidence → canonical row 有治理風險 | HIGH | 若 promotion 條件不嚴謹，可能污染 lifecycle catalog   | 需要 provenance / audit marker    |
| Forbidden language 需持續掃描               |  MED | REJECTED 文案容易被誤解成策略推薦相反面                   | 保持 HIGH=0 gate                  |
| 目前未執行 migration                        |  LOW | 符合本輪 scope，但產品頁仍不會直接出現 REJECTED rows       | 下一輪才可評估實作                       |

---

## 8. 建議今天優先處理的方向

### 第一優先：PR readiness / review for P1 plan branch

目前 `codex/p1-replay-lifecycle-rejected-canonical-promotion-plan` 已 push，但尚未開 PR。建議先做 PR readiness review：

```text
確認 diff scope 只有 2 個 outputs/replay 檔案
確認 manifest 內容與 report 一致
確認 15/26/1 統計一致
確認 forbidden-language HIGH=0
確認 validation PASS
```

若通過，再開 read-only docs PR。

### 第二優先：CTO review 15-row dry-run manifest

PR 開啟後，CTO 應審：

```text
15 rows 是否真的 promotable
是否需要 reviewer / audit marker 格式調整
是否要先做 provenance schema
是否允許下一輪進入 registry/catalog implementation
```

### 第三優先：不要直接實作 promotion

目前不建議直接進：

```text
修改 replay_strategy_registry.py
新增 catalog rows
改 API 讓 REJECTED 有資料
改 UI 顯示 REJECTED rows
```

這些應等 CTO review manifest 後再進行。

---

## 9. 下一輪可直接執行的 task prompt

```text
# ROLE
你是 LotteryNew 的 P1 REJECTED Canonical Promotion PR Readiness Reviewer，向 CTO agent 回報；CTO 向 CEO 回報。

# MISSION
針對 branch codex/p1-replay-lifecycle-rejected-canonical-promotion-plan 做 PR readiness review。
本輪只檢查 diff scope、manifest/report consistency、validation evidence、forbidden-language compliance。
若通過則開 PR。
不得修改 registry、不得新增 catalog rows、不得寫 DB、不得執行 migration。

# CONTEXT

Branch:
  codex/p1-replay-lifecycle-rejected-canonical-promotion-plan

Latest commit:
  4ff5adc docs(replay): plan rejected lifecycle canonical promotion

Current marker:
  P1_REPLAY_LIFECYCLE_REJECTED_CANONICAL_PROMOTION_PLAN_READY

Created files:
  outputs/replay/p1_replay_lifecycle_rejected_canonical_promotion_plan_20260509.md
  outputs/replay/p1_replay_lifecycle_rejected_canonical_promotion_manifest_20260509.json

Known inventory:
  Total REJECTED archive rows = 42
  Promotable = 15
  Blocked missing required fields = 26
  JSON parse error = 1
  Promotable breakdown:
    BIG_LOTTO = 13
    POWER_LOTTO = 2
    DAILY_539 = 0

Validation:
  scripts/run_replay_ci_default_validation.py = 57 passed, 32 skipped, 1 warning
  git diff --check = clean
  forbidden-language sweep HIGH=0

# REQUIRED INSPECTION

cd /Users/kelvin/Kelvin-WorkSpace/LotteryNew-main-postmerge

git fetch origin --prune
git checkout codex/p1-replay-lifecycle-rejected-canonical-promotion-plan
git status --short
git log --oneline -8

git diff --name-only origin/main...HEAD
git diff --stat origin/main...HEAD

cat outputs/replay/p1_replay_lifecycle_rejected_canonical_promotion_plan_20260509.md
cat outputs/replay/p1_replay_lifecycle_rejected_canonical_promotion_manifest_20260509.json

/Library/Developer/CommandLineTools/usr/bin/python3 scripts/run_replay_ci_default_validation.py

git diff --check

# CHECKLIST

Confirm:
1. Diff contains only the two expected outputs/replay files.
2. No code files changed.
3. No registry/catalog rows added.
4. Manifest total counts match report:
   - 42 total
   - 15 promotable
   - 26 blocked
   - 1 parse error
5. Manifest rows do not invent missing fields.
6. Blocked rows include reasons.
7. Forbidden-language sweep HIGH=0.
8. Validation remains PASS.
9. No production DB / migration / active strategy state changes.

# FORBIDDEN DIFF

Block if any diff includes:
- lottery_api/**
- index.html
- tests/**
- .github/workflows/**
- *.db
- *.db-wal
- *.db-shm
- active strategy state files
- branch protection settings
- migration scripts
- registry/catalog row implementation

# REQUIRED REPORT

Create:

outputs/replay/p1_replay_lifecycle_rejected_canonical_promotion_pr_readiness_20260509.md

Sections:
1. Executive Summary
2. Branch / Commit Verification
3. Diff Scope Review
4. Manifest Consistency Review
5. Evidence Inventory Review
6. Forbidden-Language Sweep Review
7. Validation Evidence
8. PR Readiness Decision
9. Recommended PR Title / Body
10. What Was Not Changed
11. Remaining Risks
12. Final Marker

# COMMIT RULES

Allowed commit only:
  outputs/replay/p1_replay_lifecycle_rejected_canonical_promotion_pr_readiness_20260509.md

Commit message:
  docs(replay): record rejected promotion pr readiness

Push branch to origin.

# PR CREATION

If ready:

gh pr create \
  --base main \
  --head codex/p1-replay-lifecycle-rejected-canonical-promotion-plan \
  --title "docs(replay): plan rejected lifecycle canonical promotion" \
  --body-file outputs/replay/p1_replay_lifecycle_rejected_canonical_promotion_pr_readiness_20260509.md

Do not merge PR in this round.

# FINAL MARKERS

If PR opened:
  P1_REPLAY_LIFECYCLE_REJECTED_PROMOTION_PR_OPEN

If ready but PR not opened:
  P1_REPLAY_LIFECYCLE_REJECTED_PROMOTION_PR_READY

If blocked by evidence mismatch:
  P1_REPLAY_LIFECYCLE_REJECTED_PROMOTION_PR_BLOCKED_EVIDENCE_MISMATCH

If forbidden diff found:
  P1_REPLAY_LIFECYCLE_REJECTED_PROMOTION_PR_BLOCKED_FORBIDDEN_DIFF

# FINAL REPORT FORMAT

## Completed
## Branch / Commit Verification
## Diff Scope Review
## Manifest Consistency Review
## Evidence Inventory Review
## Validation Evidence
## PR Created / Manual PR Command
## Commit / Push Result
## What Was Not Changed
## Remaining Risks
## Final Marker
```

---

## CTO Agent 10 行內摘要

1. 本輪完成 P1 REJECTED canonical promotion 的 read-only plan。
2. 已盤點 `rejected/` 42 筆 archive evidence。
3. 結果：15 promotable、26 blocked、1 JSON parse error。
4. 已產出 dry-run manifest 與 promotion plan report。
5. Branch：`codex/p1-replay-lifecycle-rejected-canonical-promotion-plan`。
6. Commit：`4ff5adc docs(replay): plan rejected lifecycle canonical promotion`。
7. Validation：`57 passed, 32 skipped, 1 warning`，`git diff --check` clean。
8. 未改 code、registry、DB、branch protection、active strategy state。
9. 下一步應做 PR readiness review，再開 read-only docs PR。
10. 不建議直接實作 promotion；需先 CTO review 15-row manifest。
